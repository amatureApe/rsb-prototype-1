import {
    ChakraProvider,
    Box,
    Stack,
    Flex,
    Container,
    Text,
    Grid,
    GridItem
} from '@chakra-ui/react'
import Layout from '../components/layout/main'
import theme from '../lib/theme'

function Staking({ Component, pageProps, router }) {
    return (
        <Box>
            Staking!
        </Box>

    )
}

export default Staking